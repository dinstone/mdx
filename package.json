{
  "name": "mdx",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @mdx/web dev",
    "dev:wails": "cd apps/wails && wails3 dev",
    "build": "pnpm --filter @mdx/web build",
    "build:wails": "cd apps/wails && wails3 build",
    "release": "cd apps/wails && wails3 package",
    "lint": "pnpm -r lint"
  }
}
